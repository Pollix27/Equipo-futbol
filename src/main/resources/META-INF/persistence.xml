<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="BaseFutbol" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entidades -->
        <class>org.futbol.equipofutbol.entidades.Equipos</class>
        <class>org.futbol.equipofutbol.entidades.Jugadores</class>
        <class>org.futbol.equipofutbol.entidades.Posiciones</class>
        <class>org.futbol.equipofutbol.entidades.Partidos</class>
        <class>org.futbol.equipofutbol.entidades.Goles</class>

        <properties>
            <!-- Conexión a MariaDB -->
            <property name="jakarta.persistence.jdbc.driver" value="org.mariadb.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mariadb://localhost:3306/BaseFutbol?createDatabaseIfNotExist=true&amp;useUnicode=true&amp;characterEncoding=UTF-8"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="12345678"/>

            <!-- Dialecto de Hibernate para MariaDB -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MariaDBDialect"/>

            <!-- Generación automática del esquema -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Logging de SQL -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.highlight_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="true"/>

            <!-- Pool de conexiones -->
            <property name="hibernate.connection.pool_size" value="10"/>

            <!-- Configuración adicional -->
            <property name="hibernate.current_session_context_class" value="thread"/>
        </properties>
    </persistence-unit>
</persistence>